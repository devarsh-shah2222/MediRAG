import { ComingSoon } from "@/components/ComingSoon";

export default function PrescriptionPage() {
  return (
    <ComingSoon
      title="Prescription Explainer"
      description="Reading a scanned prescription needs a real OCR provider, which isn't wired up yet. We're working on it -- check back soon."
    />
  );
}
