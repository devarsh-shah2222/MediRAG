import { ComingSoon } from "@/components/ComingSoon";

export default function DoctorsPage() {
  return (
    <ComingSoon
      title="Doctor & Hospital Finder"
      description="Finding real, nearby care needs a real maps/directory provider, which isn't wired up yet. We're working on it -- check back soon."
    />
  );
}
